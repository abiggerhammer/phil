module corpus.refinement_transport_composition;

component Convert[n : Nat, m : Nat](
  value : { b : Bytes[n] | n <= m },
  equality : Proof[n == m]
) {
  transport value to Bytes[m] using equality;
}
