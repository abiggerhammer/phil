module corpus.outcome_residues;

callable Read(blob : Blob) -> ReadResult {
  outcomes { success ReadOk, negative ReadFailure };
  outcome success ReadOk {
    state (blob_next : Blob);
    ensures true;
  }
  outcome negative ReadFailure {
    state ();
    ensures true;
  }
  callee preserve;
}
