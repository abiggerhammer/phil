name: Phase 1 Audit SYS-012 Erasure Authority Proof

on:
  pull_request:
    paths:
      - 'proof/Phil/Assurance/SystemsErasureAuthorityBoundary.v'
      - 'docs/phase-1/audit-sys-erasure-authority-proof-boundary-v1.md'
      - '.github/workflows/phase1-audit-sys-erasure-authority-proof.yml'
  push:
    branches: [main]
    paths:
      - 'proof/Phil/Assurance/SystemsErasureAuthorityBoundary.v'
      - 'docs/phase-1/audit-sys-erasure-authority-proof-boundary-v1.md'
      - '.github/workflows/phase1-audit-sys-erasure-authority-proof.yml'

permissions:
  contents: read

jobs:
  sys-erasure-authority-proof:
    runs-on: ubuntu-latest
    container:
      image: rocq/rocq-prover:9.2.0
      options: --user root
    steps:
      - uses: actions/checkout@v4
      - name: Compile SYS-012 imported erasure authority boundary proof
        shell: bash
        run: |
          set -euo pipefail
          export PATH="/home/rocq/.opam/${COMPILER}/bin:$PATH"
          rocq c -Q proof/Phil Phil proof/Phil/Assurance/SystemsErasureAuthorityBoundary.v
          sha256sum \
            proof/Phil/Assurance/SystemsErasureAuthorityBoundary.v \
            proof/Phil/Assurance/SystemsErasureAuthorityBoundary.vo \
            > systems-erasure-authority-proof.sha256
      - uses: actions/upload-artifact@v4
        with:
          name: systems-erasure-authority-proof
          path: systems-erasure-authority-proof.sha256
          if-no-files-found: error
