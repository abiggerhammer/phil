name: Phase 1 Callable Mode Proofs

on:
  pull_request:
    paths:
      - 'src/Phil/Core/Callable.hs'
      - 'test/Phase1CallableCaptureEffectsMain.hs'
      - 'docs/phase-1/callable-capture-effects-v1.md'
      - 'proof/Phil/Core/GenericStructural.v'
      - 'proof/Phil/Core/CallableMode.v'
      - 'app/Phase1CallableModeCertificationMain.hs'
      - '.github/workflows/phase1-callable-mode-proofs.yml'
  push:
    branches: [main]
    paths:
      - 'src/Phil/Core/Callable.hs'
      - 'test/Phase1CallableCaptureEffectsMain.hs'
      - 'docs/phase-1/callable-capture-effects-v1.md'
      - 'proof/Phil/Core/GenericStructural.v'
      - 'proof/Phil/Core/CallableMode.v'
      - 'app/Phase1CallableModeCertificationMain.hs'
      - '.github/workflows/phase1-callable-mode-proofs.yml'

permissions:
  contents: read

jobs:
  rocq-phase1-callable-mode:
    runs-on: ubuntu-latest
    container:
      image: rocq/rocq-prover:9.2.0
      options: --user root
    steps:
      - uses: actions/checkout@v4
      - name: Compile PHIL-CALL-MODE-001
        shell: bash
        run: |
          set -euo pipefail
          export PATH="/home/rocq/.opam/${COMPILER}/bin:$PATH"
          rocq c -Q proof/Phil Phil proof/Phil/Core/GenericStructural.v
          rocq c -Q proof/Phil Phil proof/Phil/Core/CallableMode.v
          mkdir -p phase1-callable-mode-proof
          cp proof/Phil/Core/CallableMode.v proof/Phil/Core/CallableMode.vo phase1-callable-mode-proof/
      - uses: actions/upload-artifact@v4
        with:
          name: rocq-phase1-callable-mode-proof
          path: phase1-callable-mode-proof
          if-no-files-found: error

  certify-phase1-callable-mode:
    needs: rocq-phase1-callable-mode
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: haskell-actions/setup@v2
        with:
          ghc-version: '9.6.7'
          cabal-version: 'latest'
      - name: Build existing proof certifier with warnings as errors
        run: cabal build exe:phil-certify-core-scalar --ghc-options=-Werror
      - name: Typecheck certifier
        run: cabal exec -- ghc -isrc -Wall -Werror -fno-code app/Phase1CallableModeCertificationMain.hs
      - name: Re-run CALL-001 through CALL-005 substrate correspondence
        run: cabal exec -- runghc -isrc test/Phase1CallableCaptureEffectsMain.hs
      - uses: actions/download-artifact@v4
        with:
          name: rocq-phase1-callable-mode-proof
          path: phase1-callable-mode-proof
      - name: Emit PHIL-CALL-MODE-001 certificate
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p phase1-callable-mode-certification
          cabal exec -- runghc -isrc app/Phase1CallableModeCertificationMain.hs \
            phase1-callable-mode-proof/CallableMode.v \
            phase1-callable-mode-proof/CallableMode.vo \
            phase1-callable-mode-certification/PHIL-CALL-MODE-001.rocq.cert
          sha256sum \
            src/Phil/Core/Callable.hs \
            test/Phase1CallableCaptureEffectsMain.hs \
            docs/phase-1/callable-capture-effects-v1.md \
            proof/Phil/Core/GenericStructural.v \
            proof/Phil/Core/CallableMode.v \
            app/Phase1CallableModeCertificationMain.hs \
            > phase1-callable-mode-certification/PHIL-CALL-MODE-001.correspondence.sha256
          cat phase1-callable-mode-certification/PHIL-CALL-MODE-001.rocq.cert
          sha256sum phase1-callable-mode-certification/PHIL-CALL-MODE-001.rocq.cert
      - uses: actions/upload-artifact@v4
        with:
          name: phil-phase1-callable-mode-proof-certification
          path: phase1-callable-mode-certification
          if-no-files-found: error
