name: SURF architecture boundary/observable bootstrap

on:
  push:
    branches:
      - phase1-surf-002-architecture-boundary-observable-parser-bootstrap
    paths:
      - architecture-boundary-observable.patch
      - .github/workflows/surf-architecture-boundary-observable-bootstrap.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Check out bootstrap branch
        uses: actions/checkout@v4
      - name: Check out target branch
        uses: actions/checkout@v4
        with:
          ref: phase1-surf-002-architecture-boundary-observable-parser
          path: target
          fetch-depth: 0
      - name: Apply architecture boundary/observable patch
        run: |
          git -C target apply --recount ../architecture-boundary-observable.patch
          git -C target diff --check
      - name: Commit architecture boundary/observable parser slice
        run: |
          cd target
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/Phil/Surface/GrammarV1/Parser.hs test/Phase1GrammarV1StaticProcessNetworkMain.hs
          git commit -m 'Complete SURF-002 architecture boundary and observable items'
          git push origin HEAD:phase1-surf-002-architecture-boundary-observable-parser
