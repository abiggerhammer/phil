name: Phase 1 INT-009 Bounded Ping Source Values

on:
  pull_request:
    paths:
      - 'src/Phil/Surface/GrammarV1/BoundedPingSourceValues.hs'
      - 'test/Phase1INT009BoundedPingSourceValuesMain.hs'
      - '.github/workflows/phase1-int009-bounded-ping-source-values.yml'
  push:
    branches: [main]
    paths:
      - 'src/Phil/Surface/GrammarV1/BoundedPingSourceValues.hs'
      - 'test/Phase1INT009BoundedPingSourceValuesMain.hs'
      - '.github/workflows/phase1-int009-bounded-ping-source-values.yml'

jobs:
  bounded-ping-source-values:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: haskell-actions/setup@v2
        with:
          ghc-version: '9.6.7'
          cabal-version: 'latest'
      - name: Build ordinary regression substrate under canonical strict warnings
        run: cabal build all --enable-tests --ghc-options=-Werror
      - name: Strictly typecheck INT-009 bounded Ping source values
        run: |
          cabal exec -- ghc -isrc -Wall -Werror -fno-code src/Phil/Surface/GrammarV1/BoundedPingSourceValues.hs
          cabal exec -- ghc -isrc -Wall -Werror -fno-code test/Phase1INT009BoundedPingSourceValuesMain.hs
      - name: Execute INT-009 bounded Ping source-value controls
        run: cabal exec -- runghc -Wall -Werror -isrc test/Phase1INT009BoundedPingSourceValuesMain.hs
